# $FreeBSD: head/devel/android-tools-adb-devel/Makefile 394371 2015-08-16 02:43:20Z jbeich $

DISTVERSION=	m-preview-890
DISTVERSIONSUFFIX=	-g94a845d
PORTREVISION=	0
PKGNAMESUFFIX=	-devel

CONFLICTS_INSTALL=	${PORTNAME}-[0-9]*

MASTERDIR=	${.CURDIR}/../android-tools-adb
DISTINFO_FILE=	${.CURDIR}/distinfo
MAKEFILE=	${.CURDIR}/files/Makefile
FILESDIR=	${.CURDIR}/files
PATCHDIR=	${.CURDIR}/files

USES=		compiler:c++11-lib
BUILD_WRKSRC=	${WRKSRC}/adb
INSTALL_WRKSRC=	${BUILD_WRKSRC}
MAKE_ENV=	FILESDIR="${FILESDIR}"
CFLAGS+=	-DADB_REVISION='\"${DISTVERSIONFULL}\"'

.include "${MASTERDIR}/Makefile"
